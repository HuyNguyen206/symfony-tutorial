{% extends 'base.html.twig' %}

{% block title %}Hello AppController!{% endblock %}
{#{% set users = [{name: 'John'}, {name: 'Susan'}] %}#}
{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name | upper }} {{ name }}! ✅</h1>
        <ul>
            {% for user in users|filter(user => user.name ) %}
                <li>
                    {{ user.name }}
                    {% include('partials/navigation.html.twig') %}
                </li>
            {% else %}
                <li>Not found</li>
            {% endfor %}
        </ul>
        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a
                            href="{{ 'D:/laragon/www/symfony-tutorial/src/Controller/AppController.php'|file_link(0) }}">src/Controller/AppController.php</a></code>
            </li>
            <li>Your template at <code><a
                            href="{{ 'D:/laragon/www/symfony-tutorial/templates/app/index.html.twig'|file_link(0) }}">templates/app/index.html.twig</a></code>
            </li>
        </ul>
    </div>
    {{ promo_code }}
{#    {% for i in 1..9 %}#}
{#        <span>{{ cycle(['huy', 'ben', 'nhung'], i) }}</span>#}
{#    {% endfor %}#}
{#    {{ app.user }}#}
    {{ app.environment }}
    {{ app.request.get('page') }}
    {{ app.request.get('limit') }}

{#    {{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}
{#    {{ form_end(form) }}#}

{#    {{ form_start(form) }}#}
{% endblock %}
    {% set va = 'this is new line' %}

{% block javascripts %}
    {{ parent() }}
        <script>
            var va = "{{ va }}"
            var test = 'another new line'
            {#alert("{{ va|escape('js') }}")#}
            // alert(test)
        </script>
{% endblock %}
